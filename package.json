{
  "name": "theolivesoft",
  "version": "1.0.0",
  "scripts": {
    "watch": "npx tailwindcss -i ./input.css -o ./output.css --watch",
    "build": "npx tailwindcss -o build.css --minify"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.14"
  }
}
